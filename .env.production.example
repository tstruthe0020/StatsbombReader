# Production Environment Variables for Soccer Foul & Referee Analytics

# ===== BACKEND CONFIGURATION =====
# GitHub Token for StatsBomb Data Access (Required)
GITHUB_TOKEN=your_github_pat_token_here

# MongoDB Connection (Required - Atlas MongoDB in production)
# Format: mongodb+srv://username:password@cluster.mongodb.net/database_name
MONGO_URL=mongodb+srv://username:password@cluster.mongodb.net/soccer_analytics

# Optional: Specify database name explicitly (if not in MONGO_URL)
DATABASE_NAME=soccer_analytics

# LLM Integration (Optional - fallback to mock responses if not provided)
EMERGENT_LLM_KEY=your_emergent_llm_key_here

# CORS Configuration (Optional - defaults to allow all in development)
# Comma-separated list of allowed origins for production
ALLOWED_ORIGINS=https://your-frontend-domain.com,https://another-allowed-domain.com

# ===== FRONTEND CONFIGURATION =====
# Backend API URL (Required for production)
REACT_APP_BACKEND_URL=https://your-backend-api-url.com

# Development overrides (not needed in production)
# DANGEROUSLY_DISABLE_HOST_CHECK=true
# HOST=0.0.0.0